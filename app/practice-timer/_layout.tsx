import { Stack } from 'expo-router';

export default function PracticeTimerLayout() {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
        animation: 'fade',
      }}
    />
  );
}
